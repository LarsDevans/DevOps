name: Create Archive

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Zip Release
      uses: TheDoctor0/zip-release@0.7.6
      with:
        directory: 'src'
